# Trip3D 功能与技术栈

## 项目定位

Trip3D 是一个面向意大利旅行的路线规划与 3D 驾驶导览项目。用户可以先筛选景点、收藏和对比目的地，再整理路线、评估每天所需时间，最后沿道路路线进入 Cesium 3D 场景。

首页采用手绘旅行手册风格，3D 导览则使用真实地形、卫星影像和建筑数据。

## 主要功能

### 景点浏览

- 按名称、城市、区域、类型和旅行偏好搜索景点。
- 查看景点简介、图片、适合时段、建议停留时间和预约提醒。
- 收藏目的地，并在旅行夹页中集中查看。
- 将多个候选景点放入对比页，比较停留时间、类型和行前提醒。

### 路线规划

- 将景点加入路线，并通过深色固定状态确认是否已加入。
- 手动调整景点顺序、锁定重要停靠点或移除景点。
- 在不移动锁定景点的前提下整理路线顺序。
- 仅在新顺序确实缩短总里程时采用优化结果。
- 使用道路级路线绘制手绘地图预览。
- 道路数据暂不可用时，显示景点之间的大致方向并给出自然提示。

### 行程时间

- 将景点建议停留时间和景点之间的驾驶时间一起计入行程。
- 跨天发生的城市转场同样计入总车程。
- 三种旅行节奏对应不同的每日时间上限：
  - `Relaxed`：6 小时。
  - `Standard`：8 小时。
  - `Fast`：10 小时。
- 自动计算建议最少天数，并提示过于紧凑的日期。
- 展示总里程、总驾驶时间、总游览时间和每天的具体安排。
- TXT 下载、打印和 PDF 使用与页面相同的时间计算结果。

### 功能服务入口

首页的六个旅行服务卡片都可以操作：

- 收藏目的地：打开收藏夹页。
- 景点对比：打开对比夹页。
- 顺路整理：整理当前路线并显示预计节省里程。
- 按天安排行程：定位到每日计划。
- 旅行资料：定位到景点资料。
- 我的旅行手册：打开账户或登录入口。

### 3D 驾驶导览

- 使用道路服务返回的路线折线驱动车辆。
- 支持自动驾驶、键盘驾驶、加速、地图视角和跟随视角。
- 在路线附近显示景点标记和可用的 GLB 景点模型。
- 显示已行驶路线、路线进度、速度、预计时间和到站提示。
- 根据瓦片加载压力自动降低模拟速度，避免车辆跑在尚未加载的地图前面。
- 场景固定在意大利白天光照基准，保证不同地区和打开时间下亮度稳定。

### 账户与持久化

- 游客状态保存在浏览器 `localStorage`。
- 登录用户可以同步路线、锁定点、收藏、对比、天数、节奏和语言。
- 未配置 PostgreSQL 时，后端账户数据可以使用 SQLite。

## 前端技术栈

| 技术 | 用途 |
| --- | --- |
| React 18 | 页面、弹层和交互组件 |
| Vite 8 | 开发服务器和生产构建 |
| CesiumJS 1.142 | 全球地形、影像、建筑和 3D 驾驶场景 |
| React Three Fiber | 首页粒子与 Three.js 场景的 React 集成 |
| Three.js | 模型、粒子和图形处理 |
| Drei | GLTF 加载等 Three.js 辅助能力 |
| Zustand | 3D 导览与路线共享状态 |
| TanStack Query | 路线数据请求、缓存和更新 |
| GSAP | 页面动效支持 |
| CSS / Tailwind PostCSS | 手绘视觉系统和响应式布局 |

## 后端与数据

| 技术或服务 | 用途 |
| --- | --- |
| FastAPI | 账户、路线代理、景点和点评接口 |
| Uvicorn | Python ASGI 服务 |
| PostgreSQL / PostGIS | 用户计划、景点和地理数据 |
| SQLite | 无 PostgreSQL 时的本地账户存储 |
| Google Routes | 配置密钥后优先提供道路路线 |
| OSRM | 道路路线回退和公开测试服务 |
| Cesium ion | World Terrain、全球影像和 OSM Buildings |
| Wikipedia / Wikidata | 景点名称、简介、图片和来源 |
| Open-Meteo | 景点天气数据 |

## 路线数据流

1. 用户在首页选择景点并确定顺序。
2. 前端将景点的可驾驶接入点发送到 `/api/routes/plan`。
3. 后端优先请求 Google Routes，未配置或请求失败时使用 OSRM。
4. 前端获得总距离、预计驾驶时间和道路折线。
5. 手绘地图与 Cesium 3D 场景共同使用这条道路折线。
6. 行程生成器结合道路时间和景点停留时间计算每天安排。

前端在本地后端不可用时还可以直接请求公共 OSRM，保证开发阶段仍能生成道路路线。

## Cesium 场景工作方式

- `CesiumDriveScene` 加载 World Terrain、World Imagery 和 OSM Buildings。
- 路线折线贴地绘制，并按进度采样车辆位置和朝向。
- 车辆高度通过最精细地形采样和相对地面高度共同处理。
- 相机根据跟随、地图和景点聚焦模式调整。
- 建筑缓存、路线简化和已走路线分块用于控制长期运行的内存占用。
- 场景时钟固定在意大利夏季中午，避免系统当前时间导致夜景或低光照。

## 视觉系统

首页遵循 `Styles/sketch.txt`：

- 暖纸白、铅笔黑、标记红、圆珠笔蓝和便签黄。
- Kalam 标题字体和 Patrick Hand 正文字体。
- 不规则圆角、粗描边和无模糊硬阴影。
- 轻微旋转、虚线、胶带与纸张纹理。
- 所有新增卡片、抽屉和状态沿用同一套设计语言。

## 目录说明

```text
backend/                    FastAPI、账户、路线与数据库接口
public/data/                已生成的景点和道路矩阵数据
public/models/              车辆与景点 GLB 模型
src/components/cesium/      Cesium 3D 驾驶场景
src/components/home/        首页、景点、路线与行程规划
src/components/ui/          3D HUD、到站卡片与信息面板
src/data/                   景点、路线和意大利轮廓数据
src/hooks/                  路线、天气和资料请求
src/state/                  Zustand 共享状态
Styles/sketch.txt           手绘视觉设计规范
tools/                      数据抓取、导入与后端启动脚本
```

## 配置与安全

- 前端需要 `VITE_CESIUM_ION_TOKEN`。
- 后端可选配置 `GOOGLE_MAPS_API_KEY` 和 `DATABASE_URL`。
- 不要提交 `.env.local`、`.env.backend`、数据库密码或真实访问令牌。
- 公共 OSRM 适合开发和低频演示，正式部署应使用自建或有服务保障的路线服务。
